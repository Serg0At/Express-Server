# 🚀 Инструкции по настройке и запуску Arbitrage Scanner API

## ✅ Что уже готово

Наш проект теперь включает в себя полноценный API с:

- **REST API** для получения арбитражных возможностей
- **WebSocket API** для стриминга данных в реальном времени
- **Swagger документацию** для удобного тестирования
- **Валидацию параметров** с использованием Joi
- **Обработку ошибок** и логирование
- **Тарифную систему** (Lite, Standard, Pro)

## 🔧 Структура проекта

```
skaner/
├── api/                    # API модули
│   ├── routes/            # REST API роуты
│   │   ├── arbitrage.js   # Роут для арбитража
│   │   └── info.js        # Роут для информации
│   ├── websocket/         # WebSocket сервер
│   │   └── arbitrageWebSocket.js
│   ├── middleware/        # Middleware
│   │   └── errorHandler.js
│   ├── validation/        # Валидация
│   │   └── arbitrageValidation.js
│   └── index.js           # Основной API роутер
├── tools/                 # Существующая логика
├── index.js               # Обновленный главный файл
├── test-api.js            # Тест REST API
├── test-client.html       # Тест WebSocket
└── README.md              # Документация
```

## 🚀 Запуск сервера

### 1. Установка зависимостей (уже выполнено)
```bash
npm install
```

### 2. Запуск сервера
```bash
npm start
```

Или в режиме разработки:
```bash
npm run dev
```

### 3. Проверка запуска
Сервер должен запуститься и вывести:
```
Server running on http://localhost:3004
API Documentation: http://localhost:3004/api/docs
WebSocket endpoint: ws://localhost:3004
```

## 🧪 Тестирование API

### Тест REST API

В новом терминале выполните:
```bash
node test-api.js
```

Ожидаемый результат:
```
🧪 Тестирование Arbitrage Scanner API

1. Получение информации о тарифах...
✅ Тарифы получены: 3 планов
   Первый план: Lite - Free

2. Получение статуса сервера...
✅ Статус сервера: running
   Время работы: X минут
   Рынков: X
   Котировок: X

3. Получение арбитражных возможностей...
✅ Арбитражные данные получены
   Найдено возможностей: X

🎉 Все тесты API прошли успешно!
```

### Тест WebSocket

1. Откройте файл `test-client.html` в браузере
2. Выберите тип подписки (Standard или Pro)
3. Настройте параметры фильтрации
4. Нажмите "Подключиться"
5. Дождитесь подтверждения подключения
6. Наблюдайте за обновлениями данных

## 📚 Доступные Endpoints

### REST API
- **GET** `/api` - Информация об API
- **GET** `/api/arbitrage` - Арбитражные возможности
- **GET** `/api/info/subscription-plans` - Тарифные планы
- **GET** `/api/info/status` - Статус сервера
- **GET** `/api/docs` - Swagger документация

### WebSocket
- **WS** `ws://localhost:3004` - Стриминг данных

## 🔍 Примеры использования

### REST API запрос
```bash
curl "http://localhost:3004/api/arbitrage?min_spread=0.5&min_volume=1.0"
```

### WebSocket подключение (JavaScript)
```javascript
const ws = new WebSocket('ws://localhost:3004');

ws.onopen = function() {
  ws.send(JSON.stringify({
    subscription_type: 'standard',
    min_spread: 0.5,
    risk: false,
    min_volume: 1.0
  }));
};

ws.onmessage = function(event) {
  const data = JSON.parse(event.data);
  console.log('Новые данные:', data);
};
```

## 🚨 Возможные проблемы и решения

### 1. Порт 3003 занят
**Решение:** Измените порт в `index.js`:
```javascript
const PORT = 3004; // или любой другой свободный порт
```

### 2. Ошибка "Cannot find module"
**Решение:** Убедитесь, что все зависимости установлены:
```bash
npm install
```

### 3. WebSocket не подключается
**Решение:** Проверьте, что сервер запущен и WebSocket сервер инициализирован

### 4. Нет данных от бирж
**Решение:** Дождитесь инициализации бирж (может занять до минуты)

## 📊 Мониторинг

### Логи сервера
Сервер выводит подробные логи:
- Инициализация бирж
- Загрузка рынков
- Обновления котировок
- WebSocket подключения
- Найденные арбитражные возможности

### Статистика
- Количество активных рынков
- Количество котировок
- WebSocket клиенты по тарифам
- Время работы сервера

## 🔒 Безопасность

- Все входные параметры валидируются
- CORS настроен для разработки
- Обработка ошибок без утечки информации

## 📈 Производительность

- WebSocket обновления по тарифам:
  - **Standard**: каждые 5 секунд
  - **Pro**: каждые 1-3 секунды
- REST API без ограничений по частоте
- Эффективная обработка данных

## 🎯 Следующие шаги

1. **Тестирование**: Запустите сервер и протестируйте все endpoints
2. **Интеграция**: Подключите API к вашему фронтенду
3. **Мониторинг**: Настройте логирование и метрики
4. **Безопасность**: Добавьте аутентификацию и rate limiting
5. **Масштабирование**: Рассмотрите использование Redis для кэширования

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь, что все зависимости установлены
3. Проверьте, что порт 3004 свободен
4. Обратитесь к документации: `http://localhost:3004/api/docs`

---

**🎉 Поздравляем! Ваш Arbitrage Scanner теперь имеет полноценный API!**
