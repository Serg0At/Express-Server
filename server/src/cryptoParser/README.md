# 🔍 Arbitrage Scanner

Сканер арбитражных возможностей на криптовалютных биржах с поддержкой REST API и WebSocket стриминга.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск сервера

```bash
npm start
```

Или в режиме разработки с автоперезагрузкой:

```bash
npm run dev
```

Сервер будет доступен по адресу: `http://localhost:3004`

## 📚 API Endpoints

### REST API

- **GET** `/api/arbitrage` - Получение арбитражных возможностей
- **GET** `/api/info/subscription-plans` - Информация о тарифах
- **GET** `/api/info/status` - Статус сервера
- **GET** `/api/docs` - Swagger документация

### WebSocket

- **WS** `ws://localhost:3004` - Стриминг арбитражных данных

## 🔧 Параметры API

### REST API параметры

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `min_spread` | number | Минимальный спред в % | 0.1 |
| `risk` | boolean | Учитывать риск | false |
| `interval` | number | Интервал в секундах (5,10,15,30) | 5 |
| `min_volume` | number | Минимальный объем | 0.001 |

### WebSocket параметры

```json
{
  "subscription_type": "standard", // "lite", "standard", "pro"
  "min_spread": 0.5,
  "risk": false,
  "min_volume": 1.0
}
```

## 💰 Тарифные планы

### Lite (Бесплатно)
- ✅ REST API доступ
- ❌ WebSocket недоступен
- 📊 Обновления каждые 30 секунд

### Standard ($29/месяц)
- ✅ REST API доступ
- ✅ WebSocket стриминг
- 📊 Обновления каждые 5 секунд

### Pro ($99/месяц)
- ✅ REST API доступ
- ✅ WebSocket стриминг
- 📊 Обновления каждые 1-3 секунды

## 🧪 Тестирование

### Тест REST API

```bash
node test-api.js
```

### Тест WebSocket

Откройте файл `test-client.html` в браузере для интерактивного тестирования WebSocket.

## 📖 Примеры использования

### JavaScript/Node.js

```javascript
// REST API
const response = await fetch('http://localhost:3004/api/arbitrage?min_spread=1.0');
const data = await response.json();
console.log(data);

// WebSocket
const ws = new WebSocket('ws://localhost:3004');
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('Новые возможности:', data);
};
```

### Python

```python
import requests
import websocket

# REST API
response = requests.get('http://localhost:3004/api/arbitrage', params={
    'min_spread': 1.0,
    'min_volume': 0.1
})
print(response.json())

# WebSocket
def on_message(ws, message):
    print(json.loads(message))

ws = websocket.WebSocketApp('ws://localhost:3004', on_message=on_message)
ws.run_forever()
```

### cURL

```bash
# Получить арбитражные возможности
curl "http://localhost:3004/api/arbitrage?min_spread=0.5&min_volume=1.0"

http://localhost:3004/api/arbitrage?min_spread=1.0

# Получить информацию о тарифах
curl "http://localhost:3004/api/info/subscription-plans"
```

## 📊 Формат данных

### Арбитражная возможность

```json
{
  "symbol": "ETH/USDT",
  "buy_exchange": "Binance",
  "buy_price": 1620.50,
  "sell_exchange": "Kraken",
  "sell_price": 1635.20,
  "spread_percent": 0.91,
  "gross_profit": 14.70,
  "net_profit_after_fees": 12.84,
  "volume": 1.0,
  "timestamp": 1695555555
}
```

## 🏗️ Архитектура

```
skaner/
├── api/                    # API модули
│   ├── routes/            # REST API роуты
│   ├── websocket/         # WebSocket сервер
│   ├── middleware/        # Middleware
│   └── validation/        # Валидация параметров
├── tools/                 # Основная логика
│   ├── exchange/          # Работа с биржами
│   ├── storage/           # Хранение данных
│   ├── watcher.js         # Мониторинг рынков
│   └── calculateProfit.js # Расчет арбитража
├── index.js               # Главный файл сервера
├── test-api.js            # Тест REST API
├── test-client.html       # Тест WebSocket
└── README.md              # Документация
```

## 🔍 Поддерживаемые биржи

- Binance
- Kraken
- Coinbase Pro
- И другие через CCXT

## 📝 Логи

Сервер выводит подробные логи в консоль:
- Инициализация бирж
- Загрузка рынков
- Обновления котировок
- Найденные арбитражные возможности
- WebSocket подключения

## 🚨 Обработка ошибок

- Валидация входных параметров
- Обработка ошибок бирж
- Graceful degradation при недоступности сервисов
- Детальные сообщения об ошибках

## 🔒 Безопасность

- Валидация всех входных параметров
- CORS настройки
- Rate limiting (планируется)
- Аутентификация (планируется)

## 📈 Мониторинг

- Статистика подключений WebSocket
- Количество активных рынков
- Время работы сервера
- Метрики производительности

## 🤝 Поддержка

- 📚 Документация: `http://localhost:3004/api/docs`
- 🐛 Issues: GitHub Issues
- 📧 Email: support@arbitrage-scanner.com

## 📄 Лицензия

ISC License
